# A1、A12、A13、A14、A19、A20 接口交接说明

## 1. 交付范围

本文件夹只包含上述六个 Story 的 C11 源码、头文件和本说明，不包含 `main()`、测试代码、构建脚本、可执行文件或编译产物。

- A1：单一命令启动并进入开局设置入口。
- A12：购买、放置和一次性触发路障。
- A13：购买并使用机器娃娃，清除前方10步内道具。
- A14：购买、放置和一次性触发炸弹，触发后住院3回合。
- A19：最终停留矿地时获得对应点数。
- A20：输入 `Quit` 强制结束整局游戏。

## 2. 文件说明

| 文件 | 用途 |
| --- | --- |
| `include/monopoly/game.h` | 公共游戏状态、上下文、玩家数组和地图。 |
| `include/monopoly/tool.h` | 玩家点数与道具库存、70格地图、购买/放置/触发/矿地接口。 |
| `include/monopoly/startup.h` | A1 启动结果与启动接口。 |
| `include/monopoly/command.h` | 统一命令分发及 A20 接口。 |
| `src/core/game.c` | 游戏状态初始化、开始、结束和当前玩家访问。 |
| `src/core/tool.c` | A12、A13、A14、A19 的核心规则实现。 |
| `src/startup/startup.c` | A1 启动检查和开局入口。 |
| `src/commands/command_dispatcher.c` | 解析 `Block n`、`Bomb n`、`Robot` 和 `Quit`。 |
| `src/commands/quit_command.c` | A20 强制结束整局游戏。 |

## 3. A1 接入

程序开始时：

```c
Game game;
char message[256];

game_init(&game);
if (application_start(&game, argc, argv, message, sizeof(message)) != STARTUP_OK) {
    /* 显示 message 后退出 */
    return 1;
}
```

成功后 `game.phase` 为 `GAME_RUNNING`，开局步骤为 `SETUP_PLAYER_COUNT`。A1 不接受额外启动参数，失败时不会留下半初始化的运行状态。

## 4. 玩家与地图接口

`game_init` 已初始化70格地图，但不会擅自创建玩家。A2 完成角色选择和资金设置后，应填写：

```c
game.player_count = 实际玩家数;
game.current_player_index = 0;
monopoly_player_init(&game.players[0], 初始资金, 初始点数, 0);
```

其余玩家同样初始化。之后可通过 `game_current_player(&game)` 获取当前玩家。

地图编号为0至69并首尾相接。医院为14号，监狱为49号。左侧6个矿地及奖励为：69号20点、68号80点、67号100点、66号40点、65号80点、64号60点。

## 5. 道具屋购买接口

道具编号、枚举和价格：

| 编号 | 枚举 | 价格 |
| ---: | --- | ---: |
| 1 | `TOOL_BLOCK` | 50点 |
| 2 | `TOOL_ROBOT` | 30点 |
| 3 | `TOOL_BOMB` | 50点 |

道具屋界面应先调用：

```c
monopoly_tool_price(tool);
monopoly_can_purchase_tool(player, tool);
monopoly_tool_shop_should_auto_exit(player);
```

用户确认购买后调用 `monopoly_purchase_tool`。购买成功会同时扣除点数并增加库存；失败时两者都不改变。玩家总道具数上限为10。点数不足30或背包已满时，自动退出道具屋。

## 6. A12、A13、A14 使用接口

主循环读取整行输入后统一调用：

```c
CommandResult result = command_execute(
    &game, input, message, sizeof(message)
);
```

分发器已经接入以下命令，均忽略大小写：

- `Block n`：在相对位置放置路障。
- `Bomb n`：在相对位置放置炸弹。
- `Robot`：清除前方1至10步内的路障和炸弹。

`n` 的合法范围为 `[-10,10]`，负数表示后方；地图位置自动按70格环绕计算。三种命令只能在 `CONTEXT_TURN_START`，即本回合掷骰前使用。成功使用后对应库存减少1。

同一位置不允许叠放多个路障或炸弹。地图显示模块可读取：

```c
game.board.cells[position].placed_tool
```

其中路障为 `TOOL_BLOCK`，炸弹为 `TOOL_BOMB`。

## 7. 移动模块的统一接入点

A8 的掷骰或 `Step n` 得到步数后，不要自行再写一套道具和矿地判断，应调用：

```c
MovementResult movement = monopoly_move_player(
    &game.board,
    game_current_player(&game),
    steps
);
```

返回结果：

- `MOVE_COMPLETED`：正常走完。
- `MOVE_STOPPED_BY_BLOCK`：停在路障位置，路障已消失。
- `MOVE_SENT_TO_HOSPITAL`：途中踩中炸弹，炸弹已消失，玩家位置改为医院14号，`hospital_turns` 为3。
- `MOVE_INVALID`：输入对象或步数无效。

炸弹放在监狱49号时也先触发炸弹，玩家进入医院，不执行坐牢。路障和炸弹均一次有效。

## 8. A19 矿地点数结算

`monopoly_move_player` 只在玩家最终停留后结算矿地，并将本次奖励写入：

```c
movement.mine_points_awarded
```

玩家更新后的总点数为 `player->points`，资金 `player->money` 不会变化。经过矿地但未停留不会奖励；同一玩家或不同玩家多次停留均可重复获得。

如其他模块直接改变了玩家最终位置，可调用 `monopoly_apply_mine_reward` 结算一次，但一次移动流程只能选择一个结算入口，不能同时再调用一次导致重复奖励。

## 9. A20 接入

`command_execute` 识别 `Quit`，不区分大小写且不接受参数。成功后：

```c
game.phase == GAME_ENDED
game.end_reason == END_REASON_USER_QUIT
```

`Quit` 应继续保留为统一分发器中的全局命令。A20 结束的是整局游戏，不是仅让当前玩家退出，也不清算当前玩家资产。

## 10. 合并注意事项

- 代码只使用标准 C11；编译时将本包的 `include` 目录加入头文件搜索路径。
- 源文件为 UTF-8，中文消息使用 `u8"..."`；Windows 最终 `main()` 负责正确显示 Unicode。
- 如果总项目已经有 `Game`、玩家、地图或命令分发器，应合并字段和逻辑，不要保留两套同名结构。
- 其他命令继续加入统一的 `command_execute`，并保留 `Quit` 的全局处理。
- A2/A4 应维护 `player_count`、`current_player_index` 和 `context`；本包不会替代玩家选择与回合切换。
- A8 应使用 `monopoly_move_player`，避免对路障、炸弹和矿地重复结算。
- `message` 可以为空；如需完整提示，建议提供至少256字节的字符数组。

## 11. 2026-08-23 与 rich-main-v1_1 的再次对照

统一主干当前使用 `Game + GameRuntime`、`GameMap/MapCell`、`runtime_*`、`a4_*` 命名。本最小交付包仍保留 `MonopolyBoard/MonopolyPlayer` 与 `monopoly_*` 作为独立 Story 测试适配层，避免为了改名破坏已通过的 A12/A13/A14/A19 测试。

已同步的兼容项：

- `Game` 增加主干同名的 `runtime`、`setup_player_count`、`setup_initial_money` 字段；
- 增加主干约定的 `startup_handle_input`（人数 -> 初始资金）；
- `Quit`、`GamePhase`、`GameContext`、`SetupStep`、`CommandResult` 保持与主干一致。

接入主干时，`monopoly_move_player` 应映射到 `runtime.c` 的 A8 `roll_and_move`；路障/炸弹映射到 `MapCell.has_block/has_bomb`，住院映射到 `a4_turn_manager_set_skip`。不要在主干再复制一套 `Game`、地图或玩家结构。详细对照见 `大富翁_C项目/docs/接口命名对照_2026-08-23.md`。

## 12. 本次改为第一版整合主干

本目录现已补入整合主干所需的 `A4`、`map`、`runtime` 文件，并以 `GameRuntime` 作为真实运行状态。道具相关功能不再只存在于旧的独立规则包：

- `Shop`：玩家到达 28 号道具屋后查看商品和库存；
- `Buy 1/2/3`：购买路障、机器娃娃、炸弹；
- `Block n` / `Bomb n`：在掷骰前放置道具；
- `Robot`：清除前方 10 步内道具；
- `Map`：地图显示路障 `#`、炸弹 `@`；
- `Step n`：测试用的确定步数，便于复现矿地和道具屋场景。

旧的 `include/monopoly/tool.h`、`src/core/tool.c` 和旧测试文件仍保留作历史参考，但已不再被 `CMakeLists.txt` 或 `scripts/build_and_test_windows.ps1` 编译。实际交付以 `runtime.c`、`map`、`A4` 和整合测试为准。

## 13. 本次主循环地图显示修正（2026-08-23）

第一版整合主干的 `src/main.c` 已统一使用 `GameRuntime` 的 `runtime_render()`：完成玩家人数和初始资金输入后自动显示地图；`Roll`、`Step`、`Buy`、`Block`、`Bomb`、`Robot` 成功后自动刷新地图；`Map` 命令仍可手动重绘。主循环消息缓冲区已扩大到 4096 字节，避免地图输出被截断。

这项改动只涉及主项目的交互入口，不改变 `runtime_*`、`a4_*`、`GameMap/MapCell` 等统一接口名称；最小交付目录仍不包含 `main()`、测试代码或构建脚本。

## 14. A4 回合顺序修正（2026-08-23）

此前运行时在到达道具屋后直接返回已处理，导致 A4 自动切换下一玩家，当前玩家无法输入 `Shop`/`Buy`。现已改为：

- 到达道具屋返回 `A4_MOVE_LANDING_PENDING`，保持当前玩家和落点处理阶段；
- `Shop` 只查看商品，不结束回合；
- `Buy 1/2/3` 成功后才完成落点事件并切换下一玩家；
- `N` 可放弃购买并完成落点事件；
- 无主土地落点同样保持 `LANDING_PENDING`，用 `Y` 购买、用 `N` 放弃，处理完成后才切换下一玩家；
- 购买完成后上一玩家不能继续执行 `Block/Bomb/Robot`，避免跨回合越权；
- 地图仍在落点、购买和道具操作后刷新。

该修正依据统一测试用例中 A4/A8/A12-A14 的“掷骰前操作 → Roll → 落点事件处理 → 下一玩家”顺序，旧整合测试中购买后继续由上一玩家放置道具的断言已调整为越权操作应被拒绝。

## 15. A19 与道具屋/礼品屋事件补全（2026-08-23）

- 停留矿地时按 `MapCell.mine_points` 累加点数，只在最终停留结算，并在提示和 `Query` 中显示更新后的点数；重复停留可重复获得。
- 进入道具屋后直接显示 1/2/3 三种道具及价格和当前点数；数字选择成功后自动退出并切换回合。点数低于最低价 30 点或背包已满时，自动退出道具屋并切换下一玩家。
- `Buy 1/2/3` 仍作为兼容别名，但统一走同一道具选择逻辑。
- 进入礼品屋后进入等待选择状态：1 奖金 +2000 元，2 点数卡 +200 点，3 财神 5 轮免过路费；选择完成后自动退出并切换回合。

## 16. 规则补充（2026-08-23）

- `Robot` 保持为掷骰前可执行的特殊命令，用于清除前方 10 步内的路障和炸弹；`Block/Bomb` 仍受掷骰前阶段限制。
- `Sell n` 将当前玩家自己的普通土地出售给银行，不需要其他玩家确认；售价为该土地投资总成本的 2 倍。特殊格、他人房产和非当前玩家掷骰前阶段均拒绝出售。
- 炸弹允许放在医院和监狱；移动处理时先检查炸弹，再处理监狱，因此监狱上的炸弹优先将玩家送入医院并住院 3 回合。
- 道具不能放在已有玩家所在的位置；玩家当前脚下的道具不会在开始移动前触发，只有后续移动进入该格时才触发。
- 道具放置后不区分放置者，任何玩家（包括原放置者）后续经过都可以触发；路障可使原放置者在矿地等位置被拦截并按最终停留规则结算。
