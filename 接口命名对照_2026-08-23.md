# 与 rich-main-v1_1 主干的接口命名对照（2026-08-23）

## 结论

本目录是 A12/A13/A14/A19 的独立测试适配包，不是主干 `rich-main-v1_1/rich-main` 的完整运行时实现。主干当前以 `Game + GameRuntime`、`GameMap/MapCell`、`runtime_*`、`a4_*` 为统一命名；本目录保留 `MonopolyBoard/MonopolyPlayer` 和 `monopoly_*` 规则接口，是为了让道具与矿地测试可以独立编译。

合并到主干时应将本包的业务规则映射到主干 `runtime.c` / `map` / `A4TurnHooks`，不要把本包的 `Game`、地图或玩家结构再次复制进主干。

## 已同步的兼容字段

- `Game` 增加 `struct GameRuntime *runtime`、`setup_player_count`、`setup_initial_money`，名称与主干一致；本适配包当前仍直接保留 `board`、`players` 供 Story 测试使用。
- 增加 `startup_handle_input`，处理主干约定的“玩家人数 -> 初始资金”引导步骤；角色选择和正式 `runtime_create` 仍由 A2/集成人接入。
- 保留 `command_execute`、`quit_command_execute`、`GamePhase`、`GameContext`、`SetupStep`、`CommandResult` 等主干同名接口。

## 业务映射

| 本适配包 | 主干对应位置 |
|---|---|
| `MonopolyPlayer.position` | `PlayerToken.position` / 主干运行时玩家位置 |
| `MonopolyBoard.cells[].placed_tool` | `MapCell.has_block`、`MapCell.has_bomb` |
| `MonopolyBoard.cells[].mine_reward` | `MapCell.mine_points` |
| `monopoly_move_player` | `runtime.c` 的 A8 `roll_and_move` 回调，必要时使用 `A4_MOVE_LANDING_PENDING` |
| `monopoly_place_tool` / `monopoly_use_robot` | A4 `run_pre_roll_action` 回调 |
| `hospital_turns` | `a4_turn_manager_set_skip(..., A4_SKIP_HOSPITAL, 3, ...)` |
| `command_execute("Quit")` | 主干全局 `Quit` 分支 + `runtime`/`a4_turn_manager_finish` |

## 本轮未改动

未重命名 `monopoly_*` 业务函数、`MonopolyBoard`、`MonopolyPlayer` 或测试文件，因为直接改名会使独立测试失去可编译性；这些名字只在本适配包内部使用，接入主干时按上表映射。
