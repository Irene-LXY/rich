# A1、A20 源码接口交接说明

## 1. 交付范围

本文件夹只提供 A1 和 A20 的 C11 源码与头文件，不包含测试代码、构建脚本、可执行文件和 `main()`。

- A1：通过单一命令启动游戏，完成启动参数检查并进入开局设置流程。
- A20：玩家输入 `Quit`，强制结束整局游戏。

## 2. 文件说明

| 文件 | 用途 |
| --- | --- |
| `include/monopoly/game.h` | 公共游戏状态、开局步骤、游戏结束原因及基础状态接口。 |
| `include/monopoly/startup.h` | A1 启动结果及 `application_start` 接口。 |
| `include/monopoly/command.h` | 命令执行结果、统一命令分发及 A20 接口。 |
| `src/core/game.c` | 游戏状态初始化、开始、结束和运行状态判断。 |
| `src/startup/startup.c` | A1 启动检查和开局流程入口。 |
| `src/commands/command_dispatcher.c` | 统一解析命令，并将 `Quit` 转交给 A20。 |
| `src/commands/quit_command.c` | A20 的具体处理逻辑。 |

## 3. A1 接口

```c
StartupResult application_start(
    Game *game,
    int argument_count,
    char *const arguments[],
    char *message,
    size_t message_size
);
```

调用前先执行：

```c
Game game;
game_init(&game);
```

然后把 `main` 收到的 `argc`、`argv` 原样传给 `application_start`。成功时返回 `STARTUP_OK`，游戏状态变为 `GAME_RUNNING`，当前开局步骤为 `SETUP_PLAYER_COUNT`。失败原因通过返回值和 `message` 给出，失败时不会生成半初始化的运行状态。

A1 要求直接运行程序，不接受额外启动参数。因此正常调用时 `argument_count` 应为 1。

## 4. A20 接口

主循环读取一整行用户输入后，统一调用：

```c
CommandResult command_execute(
    Game *game,
    const char *input,
    char *message,
    size_t message_size
);
```

当前分发器识别不区分大小写的 `Quit`，并允许命令首尾存在空白。`Quit` 后面不能带参数。命令合法时会调用 `game_end`，最终状态为：

```c
game.phase == GAME_ENDED
game.end_reason == END_REASON_USER_QUIT
```

此后再次调用命令分发器会返回 `COMMAND_GAME_ENDED`，不会继续修改游戏状态。

`quit_command_execute` 是分发器内部使用的接口。总项目通常只需从主循环调用 `command_execute`，不要在各功能中分别判断 `Quit`。

## 5. 推荐接入顺序

1. 将三个头文件放入总项目的公共头文件目录。
2. 将四个 `.c` 文件加入总项目的编译源文件列表。
3. 程序开始时创建 `Game`，先调用 `game_init`，再调用 `application_start`。
4. 游戏主循环以 `game_is_running(&game)` 为继续条件。
5. 每次读取用户命令后先交给 `command_execute`；返回 `COMMAND_OK` 且游戏已经结束时退出主循环。
6. 其他同学添加 `Roll`、`Sell`、`Query` 等命令时，应继续在统一命令分发器中扩展，保留 `Quit` 的全局优先处理。

## 6. 合并时需要注意

- 代码使用 C11，头文件引用方式为 `#include "monopoly/xxx.h"`，编译时应把本交付包的 `include` 目录加入头文件搜索路径。
- 源文件保存为 UTF-8，中文提示字符串使用 C11 的 `u8"..."`。
- Windows 最终程序的 `main()` 应负责正确显示 UTF-8；本交付包不包含界面输出层。
- 如果总项目已经定义了 `Game`、`GamePhase` 或统一命令分发器，不要直接保留两套同名结构，应由整合者把本包字段和逻辑合并到总项目现有类型中。
- `message` 可以为空；如需接收完整提示，建议调用方提供至少 256 字节的字符数组。
- A20 的含义是结束整局游戏，不是仅让当前玩家退出，也不涉及清算当前玩家资产。

## 7. 最小调用示意

```c
Game game;
char message[256];

game_init(&game);
if (application_start(&game, argc, argv, message, sizeof(message)) != STARTUP_OK) {
    /* 显示 message 后结束程序 */
    return 1;
}

while (game_is_running(&game)) {
    char input[256];
    /* 从终端读取一行到 input */
    command_execute(&game, input, message, sizeof(message));
    /* 显示 message */
}
```
