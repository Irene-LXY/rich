# 开发侧修改守则（自动化测试兼容）

> 适用对象：`rich/` 开发人员。
> 目的：在修复 bug 或开发新功能、修改现有代码时，保证程序始终兼容 `automation/` 自动化测试环境。

**核心原则：业务逻辑随便改，测试契约层不能破。**

---

## 0. 必读文档

动手前先读 `automation/README.md`，它包含：

- 程序调用方式；
- stdout 输出契约；
- 判定规则（部分匹配）；
- `map.json` 地图布局；
- 已确认的业务决策；
- 测试侧自行定义（接口规范未规定）的部分。

本守则只列“红线”和“自检要求”，细节以 `automation/README.md` 为准。

---

## 1. 不能破坏的测试入口（红线）

| 文件 / 目标 | 要求 |
| --- | --- |
| `rich/src/test_runner_main.c` | 黑盒测试入口，调用方式固定为 `<program> <test_case.json> <map.json>`，不能删除，不能改参数个数或顺序 |
| `rich/include/monopoly/automation.h` | 接口不能改；`runtime_load_preset` / `runtime_snapshot` / `runtime_finish` 的签名不能变 |
| `rich/CMakeLists.txt` | 必须保留 `monopoly_test` 可执行目标，产物名固定为 `monopoly_test`（Windows 下 `monopoly_test.exe`） |
| `rich/build.bat` | 保持一键编译，不写死编译器、不写绝对路径 |

> 这些文件/目标属于测试契约层，只能由测试人员维护。开发确需修改时，必须先和测试确认，不能自己改。

---

## 2. JSON 输入 / 输出契约

### 输入（用例的 `preset` / `actions`）

- 字段名不能改。
- 可以新增字段，但必须向后兼容：旧用例不传新字段时，程序也要能正常解析和运行。

### 输出（stdout 的 snapshot）

测试模式下，程序必须向 stdout 输出一个 JSON，二选一：

1. 完整 Actual 状态：包含 `users`、`current_user`、`phase`、`pending_prompt`、`game_status`、`winner`、`players`、`properties`、`map_items`（可选 `display_players`）；
2. 完整结果报告：`{ "result": "PASS" | "FAIL" | "ERROR", "errors": [...] }`。

这些字段名不能重命名。判定按主键部分匹配：

- `players` → 主键 `id`
- `properties` → 主键 `position`
- `map_items` → 主键 `position`
- `display_players` → 主键 `position`

这些键名是硬契约，改名会导致所有用例匹配失败。

### 调试输出

测试靠解析 stdout 的 JSON，因此：

- **禁止**把 `printf` 调试信息打到 stdout；
- 调试信息走 `stderr`，或者加开关、默认关闭。

---

## 3. 地图编号

70 格地图编号必须与 `automation/spec/map.json` 完全一致，不能随意改动：

| 编号 | 类型 |
| --- | --- |
| 0 | START |
| 1–13 | LAND_1（地段1） |
| 14 | HOSPITAL |
| 15–27 | LAND_1（地段1） |
| 28 | TOOL_SHOP |
| 29–34 | LAND_2（地段2） |
| 35 | GIFT_SHOP |
| 36–48 | LAND_3（地段3） |
| 49 | JAIL |
| 50–62 | LAND_3（地段3） |
| 63 | MAGIC_HOUSE |
| 64–69 | MINE（矿地） |

矿地点数（编号 → 点数）：64→60、65→80、66→40、67→100、68→80、69→20。

确需改地图时，必须同步修改 `map.json` 并提前通知测试，不能单独改程序侧。

---

## 4. 测试模式必须复用正式逻辑

测试入口走的是“读 JSON `preset` → 执行 `actions` → 输出快照”，它必须和正式游戏共用**同一套核心逻辑**：

- 不要为测试单独写一套假逻辑来“通过”用例；
- 修改启动流程 / 交互流程 / 回合推进时，必须同步维护 `test_runner_main.c`，保证“JSON 直达核心逻辑”仍然成立。

---

## 5. 已确认的业务规则

开发侧必须与以下规则对齐（详见 `automation/README.md`）：

- 道具价格：路障 50、机器娃娃 30、炸弹 50；
- `Quit`：强制结束整局游戏；
- 过路费不足：付款方资金扣到负数，房主只收到付款方“实际可支付的部分”；
- 刚好走到监狱才扣留 2 天，路过不停留、不入狱；
- 魔法屋：具体效果客户尚未确定，暂按“可配置清单”占位。

---

## 6. 错误处理 / 边界行为

测试支持 `expected_result` / `expected_errors`（缺省为 PASS；`ERROR` 表示期望程序报错）。

如果开发把“原来报错的场景”改成不报错，或反过来，必须通知测试，因为相关边界用例的期望值需要同步调整。

---

## 7. 分工边界

- 开发只改 `rich/`；
- **不要动 `automation/`**（运行器、`spec/map.json`、`testcases/`），除非和测试确认后一起改。

---

## 8. 提交前自检

每次修改、提交前至少执行：

```bat
cd rich
build.bat

cd ..\automation
run_tests.bat
```

要求：

- `build.bat` 能正常生成 `monopoly_test`（可能位于 `build\Debug` 或 `build\Release`）；
- `run_tests.bat` 跑完后，正例 PASS 数不下降，尤其不能把原本 PASS 的用例变成 ERROR；
- 新功能需要新用例时，先找测试一起补用例，不要自行修改测试环境来“配合”程序。
