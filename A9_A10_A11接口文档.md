# A9/A10/A11 房产接口文档

> 本目录接在 A8 后开发。根目录 `roll.h`、`new.c`、`A8_Roll接口文档.md` 不修改。

## 1. 目录与职责

```text
A9_A10_A11/
├─ include/property_system.h       对外接口
├─ src/property_system.c           A9/A10/A11 实现
├─ tests/test_property_system.c    自动化测试
├─ docs/A9_A10_A11接口文档.md      本文档
└─ CMakeLists.txt                  独立构建入口
```

- A9：购买无主土地、将自有房产从 0 级升级到 3 级。
- A10：收取累计投资一半的过路费，处理住院/入狱和财神免租。
- A11：当前玩家在 Roll 前通过 `Sell n` 语义出售自有房产。

## 2. 与 A8 的调用顺序

A8 会在 `roll_handle_command` 成功后立即切换 `current_player`，所以调用方必须先保存移动玩家下标：

```c
int moving_player = gs.current_player;
MoveResult move;
PropertyResult property;

if (!property_can_roll(&property_system, &gs, moving_player)) {
    /* 先处理尚未完成的 Y/N。 */
}

if (roll_handle_command(&gs, "Roll", dice, &move) == ROLL_OK) {
    PropertyCode code = property_after_move(
        &property_system, &gs, moving_player, &move, &property);
    if (code == PROPERTY_PENDING) {
        /* 根据 property.action 提示购买或升级，并等待 Y/N。 */
    }
}
```

出现 `PROPERTY_PENDING` 后，必须调用：

```c
property_resolve_pending(&property_system, &gs, yes_or_no, &property);
```

在确认完成前，`property_can_roll` 返回 0，防止下一玩家提前 Roll。

## 3. 土地价格约定

新模块不改变 A8 的 `Cell`：

- `Cell.level`：0/1/2/3，分别表示空地、茅屋、洋房、摩天楼；
- `Cell.value`：有主房产时保存累计投资，使 A8 的 `value / 2` 与 A10 房租一致；
- `PropertySystem.base_price[position]`：保存原始地价；
- 出售后格子恢复 `CELL_EMPTY`，`Cell.value` 恢复原始地价。

地图初始化时可调用：

```c
property_configure_land(&property_system, &gs, position, price);
```

项目 PDF 的地价为：地段一 200 元、地段二 500 元、地段三 300 元。若地图模块已把无主地价格写入 `Cell.value`，也可以在地图完成后直接调用 `property_system_init` 自动读取。

## 4. 核心接口

```c
void property_system_init(PropertySystem*, const GameState*);
PropertyCode property_configure_land(PropertySystem*, GameState*,
                                     int position, int price);
PropertyCode property_after_move(PropertySystem*, GameState*,
                                 int moving_player_index,
                                 const MoveResult*, PropertyResult*);
PropertyCode property_resolve_pending(PropertySystem*, GameState*,
                                      int accept, PropertyResult*);
PropertyCode property_sell(PropertySystem*, GameState*,
                           int player_index, int position,
                           PropertyResult*);
int property_has_pending(const PropertySystem*);
int property_can_roll(const PropertySystem*, const GameState*, int player_index);
```

所有接口返回 `PropertyCode`。错误时不应部分修改房产和资金。

## 5. A9 规则

```text
购买费用 = 基础地价
每次升级费用 = 基础地价
累计投资 = 基础地价 × (level + 1)
```

- 落在无主 `CELL_EMPTY` 且资金足够：返回 `PROPERTY_PENDING` 和 `PROPERTY_ACTION_BUY`。
- 落在自有 `CELL_HOUSE` 且等级小于 3、资金足够：返回 `PROPERTY_PENDING` 和 `PROPERTY_ACTION_UPGRADE`。
- `accept=0` 放弃，不扣款；`accept!=0` 执行交易。

## 6. A10 规则

A8 已在 `LAND_OTHER_HOUSE` 分支按 `Cell.value / 2` 扣款。新模块把 `Cell.value` 与累计投资同步，因此正常房租无需重复扣款。

`property_after_move` 会核对 A8 的 `MoveResult.toll`：

- 房主 `hospital_days > 0` 或 `prison_days > 0`：退回 A8 已扣房租；
- 付款玩家财神剩余轮数大于 0：退回 A8 已扣房租，UI 应显示“财神附体，免交房租！”；
- 其他情况：最终房租为累计投资的一半。

财神接口：

```c
property_set_fortune_turns(&property_system, player_index, 5U);
property_get_fortune_turns(&property_system, player_index);
```

## 7. A11 规则

```text
出售价格 = 累计投资 × 2
```

`property_sell` 只在以下条件全部满足时成功：

- 没有等待处理的 A9 `Y/N`；
- `player_index == gs.current_player`；
- `gs.has_moved == 0`；
- `position` 是该玩家自己的 `CELL_HOUSE`。

出售不切换玩家、不消耗 Roll；上层把命令 `Sell n` 解析为绝对地图位置后调用本接口。

## 8. 后续 Story 接口

- 地图模块：负责调用 `property_configure_land` 配置每块地的价格。
- 礼品屋模块：获得财神时调用 `property_set_fortune_turns(..., 5U)`。
- Query 模块：可读取 `GameState.players`、`GameState.board` 和 `PropertySystem.fortune_turns` 展示资产。
- 破产模块：A10 结算后若资金小于 0，应统一宣布破产并把该玩家房产逐块恢复为 `CELL_EMPTY`；不要修改 A8 Roll 接口。
